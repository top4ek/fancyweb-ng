import { HeaderMenuList } from './types';

export default function HeaderMenu(props: HeaderMenuList) {
  const { list } = props;
  console.log(list);
  return (
    <nav>
      <ul>
      </ul> 
    </nav>
  );
}
